#!/usr/bin/env python3
"""Mock relion_python_topaz (the conda-env wrapper the autopick Topaz mode
wraps via --fn_topaz_exe). Exists so the t264 probe inventory finds topaz on
the cluster; the mock relion_autopick does the picking itself."""
import sys
print("cryoflow-mock topaz: CNN picking would happen here", " ".join(sys.argv[1:]))
sys.exit(0)
